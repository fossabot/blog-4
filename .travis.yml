language: go

sudo: false

dist: trusty

go:
  - "1.10.1"

os:
  - linux

install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq rsync sshpass hugo

script:
  - ./hugo

after_success:
  - sshpass -p $SFTP_PASSWORD rsync -arvzc ./public/* $SFTP_USER@codeep.blue:/var/www
